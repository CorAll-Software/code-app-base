# Dependencias
node_modules

# Entorno (example.env es el que se versiona, sin valores)
.env
.env.local

# Salida de build (`build.ts` sí se versiona: es el script, no la salida)
/build

# Logs y volcados
*.log
*.trace
**/*.zip
**/*.tar.gz
**/*.tgz

# Editor / SO
.DS_Store
*.pem
