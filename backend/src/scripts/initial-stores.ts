// import { execProcedure } from "@core/db/connection"
// import { usersStore } from "@core/store"

export const load_stores = async () => {

    // const results = await Promise.allSettled([
    //     execProcedure('segurity.read_users', [{}]),
    // ]) as PromiseSettledResult<any>[]

    // for (const [i, r] of results.entries()) {

    //     if (r.status !== 'fulfilled' || !r.value?.result) continue

    //     const { result } = r.value

    //     if (!result) continue

    //     if (i === 0) {
    //         //* Carga los usuarios
    //         for (const u of result) {
    //             usersStore.set(u.id, u)
    //         }
    //         console.log('Usuarios', usersStore.size)
    //     }

    // }

    return true

}