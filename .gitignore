# Dependencias
node_modules

# Entorno (los secretos NUNCA se versionan; example.env documenta las claves)
.env
.env.local
backend/.env

# Configuración de sentry-cli: la lee del directorio de trabajo y lleva el
# token de subida de sourcemaps en `[auth] token=`.
.sentryclirc

# Salidas de build
build
dist
.turbo
*.tsbuildinfo

# Logs
*.log

# Editor / SO
.DS_Store
Thumbs.db
.vscode
.idea
*.pem

# Agentes / herramientas
.claude
.agents
skills-lock.json
tmp
